#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

override_dh_auto_configure:
	cd pocket && autoreconf -fiv
	dh_auto_configure -O-Dpocket

%:
	dh $@ -D pocket
